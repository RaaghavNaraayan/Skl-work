#define a python function called absolute that takes 1 parameter x and returns the absolute value of x. Write the code in a module and return the result to the main program.
def abs(n):
    n=n*-1
    print(n)
n=int(input("Enter a number: "))

